if not locale then
  locale = {}
end
if not locale.languages then
  locale.languages = {}
end
locale.languages.ENGLISH = {
  codePage = "fonts/fonts_euro",
  uiResources = "ui/locale/english/uiResources.txt",
  uiName = "Menu_ENGLISH",
  sortId = 1,
  shortName = "en"
}
